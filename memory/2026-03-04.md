# Daily Notes — March 4, 2026

## Media Detox — DAY 2
- Check in daily per agreement

---

## Wren Health — Full Day of Bug Fixes

### SQL / Supabase
- ✅ `calendar_participants` table already existed (ran previously)
- ✅ `avatars` storage bucket created + RLS policies applied
- ✅ All pending SQL tasks cleared

### Bug Fixes Shipped
- ✅ SetupSelfScreen: "Skip for Now" → "Save for Later"
- ✅ Date pickers (all screens): replaced `> 1971` epoch guard with `minimumDate={new Date(1900,0,1)}` + height bumped 120→200 for all pickers
- ✅ AddEditMemberScreen: DOB replaced text input with scrollable DateTimePicker spinner
- ✅ AppointmentsScreen: past appointments now render ABOVE upcoming (scroll up to find), greyed out, UPCOMING divider — matches CalendarScreen layout
- ✅ Time picker COMPLETELY REPLACED: `@react-native-community/datetimepicker` spinner was broken on iOS (locked to 4pm = epoch in PST). Built custom `TimePickerWheel` component (`src/components/TimePickerWheel.tsx`) — two ScrollView columns (hours 1-12, minutes 00-59) + AM/PM toggle. 100% under our control.
- ✅ Date/time state split into separate `pickerDate` + `pickerHour12` + `pickerMinute` + `pickerAmPm` — no cross-contamination between date and time
- ✅ UTC timezone bug fixed: `toDateKey()` now uses local date components instead of `.toISOString().split('T')[0]` — was showing one day off in calendar list vs detail
- ✅ Calendar appointments auto-assign all family members by default in "Who is this for" — was defaulting to none, so nothing appeared in health accounts

### Known Patterns
- Metro cache caused multiple false "still broken" reports — always try `npx expo start --clear` before assuming code is wrong
- `@react-native-community/datetimepicker` v8.4.4 spinner mode on iOS is fundamentally broken for time picking — avoid it, use custom component

### End of Day Status (Morning)
- App is working and being stress-tested with 5 demo accounts
- All major bugs from morning session resolved

---

## Afternoon Session — Phone Testing (Release Build on Device)

Cosmo ran the app on physical iPhone via Xcode Release build. Found 14 issues:

### Fixes IN PROGRESS (session cut off mid-work)

**Completed:**
- ✅ #3 Settings "Share with a Friend" → wired up iOS native Share sheet
- ✅ #4 Help/Privacy/Terms links → already correct (wrenhealth.app/help, /privacy, /terms) — website routing issue, not app
- ✅ #5 Appointments + button not centered → styled as proper green circle with centered icon
- ✅ #6 ShareAccountScreen permissions → replaced 8 individual field toggles with 6 section-level toggles (General Info, Medical Info, Insurance, Emergency Contacts, Physicians & Care Providers, Appointments) — all on by default
- ✅ #7 Camera icon tappable on shared accounts → disabled when `member.owner_id !== session.user.id`
- ✅ #2 Photo upload "bucket not found" → RLS policy was blocking: path was `${memberId}/...` but policy requires `auth.uid()` as folder. Fixed to `${session.user.id}/${memberId}_${timestamp}.${ext}`
- ✅ #9 AddEventModal keyboard covers Location/Notes → added KeyboardAvoidingView
- ✅ #10 CalendarSettings keyboard covers invite email input → added KeyboardAvoidingView
- ✅ #11 Duplicate accept + badge not clearing → upsert with ignoreDuplicates, badge immediately cleared via setPendingInvites filter, onJoined now passes inviteId
- ✅ #13 Calendar settings "(you)" label + names not showing → removed (you), updated loadAllParticipants to include ALL calendar owners/members (not just those with sub-accounts in calendar_participants)
- ✅ #14 No way to add sub-accounts after joining → "Manage my accounts" button added in CalendarSettingsModal under current user's group, opens SubAccountPickerModal
- ✅ #8 SubAccountPickerModal invite section → adding invite-by-email UI (IN PROGRESS when session cut)

**Remaining / Not yet started:**
- [ ] #1 Auth screen background graphics (visual polish) — low priority
- [ ] #12 "Who is this for" only shows own sub-accounts — RLS blocks reading other users' family_members in join. Needs SQL fix: new RLS policy on family_members allowing read if member is in a shared calendar. SQL needed:
  ```sql
  CREATE POLICY "family_members_calendar_read" ON public.family_members
    FOR SELECT USING (
      owner_id = auth.uid()
      OR id IN (
        SELECT cp.family_member_id FROM public.calendar_participants cp
        WHERE cp.calendar_id IN (
          SELECT id FROM public.family_calendars WHERE owner_id = auth.uid()
          UNION
          SELECT calendar_id FROM public.calendar_members WHERE user_id = auth.uid()
        )
      )
    );
  ```
- [ ] SubAccountPickerModal invite send function (was mid-edit when session compacted)
- [ ] TypeScript check after all changes
- [ ] Git commit + push everything

### Other Notes
- Cosmo used `npx expo run:ios --device` then switched to Xcode Release build for device testing
- Release build bundles JS — no Metro needed
- calendar_participants upsert needed: `confirm()` in SubAccountPickerModal should upsert to handle re-opening from "Manage my accounts"
